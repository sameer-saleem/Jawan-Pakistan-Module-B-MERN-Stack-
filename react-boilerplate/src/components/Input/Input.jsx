import React from 'react';
import { TextField } from '@mui/material';

const Input = () => {
  return (
    <TextField
      label="Input"
      variant="outlined"
      fullWidth
    />
  );
};

export default Input;
